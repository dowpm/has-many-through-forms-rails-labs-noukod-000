<h2><%= @post.title %></h2>
<h2><%= @post.content %></h2>
<% if @post.categories.size > 0 %>
Categories:
<ul>
  <%= @post.categories.each do |category| %>
    <li><%= link_to category.name, category %>  </li>
  <% end %>
</ul>
<% end %>


